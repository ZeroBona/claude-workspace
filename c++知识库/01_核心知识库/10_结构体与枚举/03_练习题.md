# 练习题 📝

## ⭐ 基础题

### 第1题：定义图书结构体
定义一个 `Book` 结构体，包含书名、作者、价格三个成员。创建一本书并输出信息。

### 第2题：学生信息
定义 `Student` 结构体（姓名、年龄、成绩），创建 3 个学生，输出成绩最高的那个。

## ⭐⭐ 进阶题

### 第3题：用函数操作结构体
编写函数 `void printBook(Book b)` 输出图书信息。定义一个图书数组，遍历调用函数输出。

### 第4题：星期枚举
定义枚举 `Weekday`（MON=1 到 SUN=7），输入数字输出对应的星期英文缩写。

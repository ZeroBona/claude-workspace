# 代码示例 💻

---

## 示例 1：写入和读取文件 📝

**文件**：[code/01_file_io.cpp](code/01_file_io.cpp)

```cpp
#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    ofstream fout("note.txt");
    fout << "C++ 文件操作学习笔记" << endl;
    fout << "1. ofstream 写入" << endl;
    fout << "2. ifstream 读取" << endl;
    fout.close();

    ifstream fin("note.txt");
    string line;
    cout << "文件内容：" << endl;
    while (getline(fin, line)) {
        cout << line << endl;
    }
    fin.close();

    return 0;
}
```

**运行效果**：
```
文件内容：
C++ 文件操作学习笔记
1. ofstream 写入
2. ifstream 读取
```

---

## 示例 2：保存成绩到文件 📊

**文件**：[code/02_save_scores.cpp](code/02_save_scores.cpp)

```cpp
#include <iostream>
#include <fstream>
using namespace std;

struct Student {
    string name;
    int score;
};

int main() {
    const int N = 3;
    Student students[N];

    for (int i = 0; i < N; i++) {
        cout << "请输入第" << i+1 << "个学生姓名和成绩：";
        cin >> students[i].name >> students[i].score;
    }

    ofstream fout("scores.txt");
    for (int i = 0; i < N; i++) {
        fout << students[i].name << " " << students[i].score << endl;
    }
    fout.close();
    cout << "成绩已保存到 scores.txt" << endl;

    return 0;
}
```

---

## 示例 3：追加日志 🔄

**文件**：[code/03_append_log.cpp](code/03_append_log.cpp)

```cpp
#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    ofstream fout("log.txt", ios::app);
    fout << "程序运行了一次" << endl;
    fout.close();

    ifstream fin("log.txt");
    string line;
    cout << "=== 运行记录 ===" << endl;
    while (getline(fin, line)) {
        cout << line << endl;
    }
    fin.close();

    return 0;
}
```

---

## 示例 4：读写数字文件 🔢

**文件**：[code/04_read_numbers.cpp](code/04_read_numbers.cpp)

```cpp
#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ofstream fout("numbers.txt");
    int nums[] = {10, 20, 30, 40, 50};
    for (int i = 0; i < 5; i++) fout << nums[i] << " ";
    fout.close();

    ifstream fin("numbers.txt");
    int x, sum = 0, count = 0;
    while (fin >> x) {
        cout << x << " ";
        sum += x;
        count++;
    }
    fin.close();
    cout << "\n个数：" << count << " 总和：" << sum << endl;

    return 0;
}
```

---

## 示例 5：文件复制 📋

**文件**：[code/05_copy_file.cpp](code/05_copy_file.cpp)

```cpp
#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {
    string filename;
    cout << "请输入要复制的文件名：";
    cin >> filename;

    ifstream fin(filename);
    if (!fin.is_open()) { cout << "文件打开失败！" << endl; return 1; }

    ofstream fout("copy_" + filename);
    string line;
    while (getline(fin, line)) fout << line << endl;

    fin.close();
    fout.close();
    cout << "文件已复制为 copy_" << filename << endl;

    return 0;
}
```

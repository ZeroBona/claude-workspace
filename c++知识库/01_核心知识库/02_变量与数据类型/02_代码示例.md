# 代码示例 💻

---

## 示例 1：变量初体验 🎯

**文件**：[code/01_variable_intro.cpp](code/01_variable_intro.cpp)

```cpp
#include <iostream>
using namespace std;

int main() {
    // 定义变量
    int age = 12;
    double height = 155.5;
    char grade = 'A';
    bool isStudent = true;
    string name = "小明";

    // 输出变量的值
    cout << "姓名：" << name << endl;
    cout << "年龄：" << age << "岁" << endl;
    cout << "身高：" << height << "cm" << endl;
    cout << "成绩等级：" << grade << endl;
    cout << "是学生吗？" << isStudent << "（1表示真，0表示假）" << endl;

    return 0;
}
```

**运行效果**：
```
姓名：小明
年龄：12岁
身高：155.5cm
成绩等级：A
是学生吗？1（1表示真，0表示假）
```

---

## 示例 2：变量可以变 🔄

**文件**：[code/02_var_change.cpp](code/02_var_change.cpp)

```cpp
#include <iostream>
using namespace std;

int main() {
    int x = 5;

    cout << "一开始，x = " << x << endl;

    x = 10;
    cout << "改成 10，x = " << x << endl;

    x = x + 3;
    cout << "加 3，x = " << x << endl;

    x = x * 2;
    cout << "乘以 2，x = " << x << endl;

    x = x - 8;
    cout << "减 8，x = " << x << endl;

    return 0;
}
```

**运行效果**：
```
一开始，x = 5
改成 10，x = 10
加 3，x = 13
乘以 2，x = 26
减 8，x = 18
```

---

## 示例 3：输入各种类型的数据 ⌨️

**文件**：[code/03_input_types.cpp](code/03_input_types.cpp)

```cpp
#include <iostream>
using namespace std;

int main() {
    string name;
    int age;
    double score;
    char grade;

    cout << "请输入你的姓名：";
    cin >> name;

    cout << "请输入你的年龄：";
    cin >> age;

    cout << "请输入你的数学成绩：";
    cin >> score;

    cout << "请输入你的等级（A/B/C/D）：";
    cin >> grade;

    cout << endl;
    cout << "=== 学生信息 ===" << endl;
    cout << "姓名：" << name << endl;
    cout << "年龄：" << age << "岁" << endl;
    cout << "数学成绩：" << score << "分" << endl;
    cout << "等级：" << grade << endl;

    return 0;
}
```

**运行效果**：
```
请输入你的姓名：小红
请输入你的年龄：13
请输入你的数学成绩：95.5
请输入你的等级（A/B/C/D）：A

=== 学生信息 ===
姓名：小红
年龄：13岁
数学成绩：95.5分
等级：A
```

---

## 示例 4：常量的使用 🔒

**文件**：[code/04_const_demo.cpp](code/04_const_demo.cpp)

```cpp
#include <iostream>
using namespace std;

int main() {
    const double PI = 3.14159;
    const int DAYS_IN_YEAR = 365;

    int r;
    cout << "请输入圆的半径：";
    cin >> r;

    double area = PI * r * r;
    double circumference = 2 * PI * r;

    cout << "半径为" << r << "的圆：" << endl;
    cout << "面积 = " << area << endl;
    cout << "周长 = " << circumference << endl;
    cout << "一年有" << DAYS_IN_YEAR << "天" << endl;

    // PI = 3.14;  // ❌ 这行会报错！常量不能修改

    return 0;
}
```

**运行效果**：
```
请输入圆的半径：5
半径为5的圆：
面积 = 78.5397
周长 = 31.4159
一年有365天
```

---

## 示例 5：变量交换 🤝

**文件**：[code/05_swap.cpp](code/05_swap.cpp)

```cpp
#include <iostream>
using namespace std;

int main() {
    int a = 10;
    int b = 20;

    cout << "交换前：a = " << a << "，b = " << b << endl;

    // 用第三个变量来交换
    int temp = a;   // temp = 10
    a = b;          // a = 20
    b = temp;       // b = 10

    cout << "交换后：a = " << a << "，b = " << b << endl;

    return 0;
}
```

**运行效果**：
```
交换前：a = 10，b = 20
交换后：a = 20，b = 10
```

> 💡 **小技巧**：交换两个变量的值，需要用到**第三个"空盒子"**临时存放。
> 就像交换两杯饮料，需要先倒一杯到空杯子里！

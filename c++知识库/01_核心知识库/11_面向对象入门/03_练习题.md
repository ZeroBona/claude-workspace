# 练习题 📝

## ⭐ 基础题

### 第1题：狗狗类
定义一个 `Dog` 类，有 name 和 age 私有成员，通过构造函数初始化，有 `bark()` 成员函数输出"汪汪！"。

### 第2题：矩形类
定义 `Rectangle` 类，有长和宽私有成员，有 `area()` 和 `perimeter()` 成员函数计算面积和周长。

## ⭐⭐ 进阶题

### 第3题：银行账户类
定义 `BankAccount` 类：
- 私有成员：账户名、余额
- 公有方法：存款、取款、查询余额
- 取款时判断余额是否足够
